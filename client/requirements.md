## Packages
framer-motion | Animation library for smooth transitions and "scanning" effects
date-fns | Date formatting for timestamps
clsx | Conditional class merging
tailwind-merge | Class merging utility

## Notes
API endpoints are defined in shared/routes.ts
Authentication is assumed to be handled via cookies (credentials: "include")
